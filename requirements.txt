# ── Deep learning ────────────────────────────────────────────────────────────
# Default pip install gives the CPU build.
# For GPU (Azure / CUDA 12.x), install torch separately BEFORE this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
numpy>=1.26.0

# ── HuggingFace / Transformers ───────────────────────────────────────────────
transformers>=4.30.0
sentencepiece>=0.1.99   # required for DeBERTa-v3 (SentencePiece tokenizer)

# ── Machine learning ─────────────────────────────────────────────────────────
scikit-learn>=1.3.0

# ── Visualisation (stage2 EDA) ───────────────────────────────────────────────
matplotlib>=3.7.0

# ── Progress bars (used internally by transformers / torch) ──────────────────
tqdm>=4.65.0
